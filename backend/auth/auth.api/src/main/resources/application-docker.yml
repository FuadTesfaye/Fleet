spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/fms_auth
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:29092
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration

server:
  port: 8081
